// using System.Collections;
// using System.Collections.Generic;
using UnityEngine;

public class NpcBehaviour_test : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {

        // Log message of Npc status
        Debug.Log("Npc Successfully Loaded");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
